diff -Nur sr5sieve-1.8.10-src-orig/config.h sr5sieve-1.8.10-src/config.h
--- sr5sieve-1.8.10-src-orig/config.h	2009-09-16 17:01:10.000000000 -0400
+++ sr5sieve-1.8.10-src/config.h	2010-11-22 21:41:07.083351002 -0500
@@ -17,27 +17,10 @@
 # define attribute(list)
 #endif
 
-/* Define HAVE_FORK if unistd.h declares fork()
- */
-#ifndef HAVE_FORK
-# if BOINC
-#  define HAVE_FORK 0
-# elif defined(_WIN32)
-#  define HAVE_FORK 0
-# else
-#  define HAVE_FORK 1
-# endif
-#endif
-
-/* Define HAVE_GETRUSAGE if sys/resource.h declares getrusage()
- */
-#ifndef HAVE_GETRUSAGE
-# if defined(_WIN32)
-#  define HAVE_GETRUSAGE 0
-# else
-#  define HAVE_GETRUSAGE 1
-# endif
-#endif
+#define HAVE_FORK 		0
+#define HAVE_GETRUSAGE 		0
+#define HAVE_MALLOPT 		0
+#define HAVE_SETAFFINITY	0
 
 /* Define HAVE_GETTIMEOFDAY if sys/time.h declares gettimeofday()
  */
@@ -59,16 +42,6 @@
 # endif
 #endif
 
-/* Define HAVE_SETAFFINITY if sched.h declares sched_setaffinity().
- */
-#ifndef HAVE_SETAFFINITY
-# if defined(_WIN32) || defined(__APPLE__) || defined(__HAIKU__)
-#  define HAVE_SETAFFINITY 0
-# else
-#  define HAVE_SETAFFINITY 1
-# endif
-#endif
-
 /* Define HAVE_ALARM if signal.h defines SIGALRM
  */
 #ifndef HAVE_ALARM
@@ -118,26 +91,6 @@
 # endif
 #endif
 
-/* Define HAVE_MMAP if sys/mman.h declares a mmap() function.
- */
-#ifndef HAVE_MMAP
-# if defined(_WIN32)
-#  define HAVE_MMAP 0
-# else
-#  define HAVE_MMAP 1
-# endif
-#endif
-
-/* Define HAVE_MALLOPT if malloc.h declares a mallopt() function.
- */
-#ifndef HAVE_MALLOPT
-# if defined(_WIN32) || defined(__APPLE__) || defined(__HAIKU__)
-#  define HAVE_MALLOPT 0
-# else
-#  define HAVE_MALLOPT 1
-# endif
-#endif
-
 #if defined(__HAIKU__)
 #define WAIT_ANY 1
 #endif
diff -Nur sr5sieve-1.8.10-src-orig/Makefile sr5sieve-1.8.10-src/Makefile
--- sr5sieve-1.8.10-src-orig/Makefile	2009-09-16 17:27:52.000000000 -0400
+++ sr5sieve-1.8.10-src/Makefile	2010-11-22 21:38:39.155351002 -0500
@@ -22,7 +22,7 @@
 #ARCH=ppc64-osx     # Apple G5 with MacOS X
 #ARCH=ppc64-linux   # Apple G5 with Linux
 #ARCH=i586          # Generic Pentium or compatible
-#ARCH=i686          # Pentium 2 or Pentium 3
+ARCH=i686          # Pentium 2 or Pentium 3
 #ARCH=athlon        # Athlon (32-bit)
 #ARCH=pentium4      # Pentium 4 (32-bit)
 #ARCH=k8            # Athlon 64 (Also Core 2) in native 64-bit mode, SSE2 only.
diff -Nur sr5sieve-1.8.10-src-orig/sr5sieve.h sr5sieve-1.8.10-src/sr5sieve.h
--- sr5sieve-1.8.10-src-orig/sr5sieve.h	2009-02-16 06:37:49.000000000 -0500
+++ sr5sieve-1.8.10-src/sr5sieve.h	2010-11-22 21:38:39.155351002 -0500
@@ -18,6 +18,8 @@
 #define XSTR(ARG) STR(ARG)
 #define STR(ARG) #ARG
 
+#define BASE 0
+
 /* Also check config.h for compiler/OS settings.
  */
 
