#ifndef _NEXUS_FILE_UTIL_HH
#define _NEXUS_FILE_UTIL_HH

#include <string>
#include <assert.h>

template <class F> 
static inline void forlines(const std::string &fname, F &f) {
  assert(0);
}

#endif // _NEXUS_FILE_UTIL_HH
